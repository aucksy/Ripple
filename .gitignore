# Kept out of this public repository on purpose: the overview document names
# internal hostnames and internal approval processes.
Ripple - Overview.pdf
ripple-overview.html

# Real code from work, kept here to test the reader against. Internal table and
# dataset names, so it never goes near a public repository. Anything learned
# from it belongs in the tests as neutral names and shapes, never verbatim.
Docs/

Codebase/.venv/
Codebase/ripple.db
**/__pycache__/
*.pyc
.pytest_cache/
.vercel/
.env

# Photographs taken on a phone, of a template from work. Same reason as Docs/:
# this repository is public, and what is in the pictures is not.
PPT/

# A whole working copy of Ripple, rebuilt from the repair kit to try something
# out. It carries its own vendored sqlglot and a packaged build, which is
# tens of megabytes of files this repository already holds the source of.
# Kept on disk, kept out of git. See "never commit a build artefact".
RIPPLE COPILOT DEMO/
